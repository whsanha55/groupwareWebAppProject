<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bit.groupware.persistent.mapper.ArticleMapper">
<!-- 
	<insert id="insertArticle" parameterType="Article" statementType="PREPARED">			
		<selectKey keyProperty="no" resultType="int" statementType="STATEMENT" order="BEFORE">
			select article_seq.nextval from dual		
		</selectKey>				
		insert into article (no, writer, subject, content, pwd, writeday, hitcount)
		values (#{no}, #{writer}, #{subject}, #{content}, #{pwd}, sysdate, 0)	
	</insert>	
	

	<select id="selectArticleList" resultType="Article" statementType="STATEMENT">
		select 
			no, 
			writer,
			subject,
			to_char(writeday, 'YYYY/MM/DD') as writeday,
			hitCount 
		from article
		order by writeday desc	
	</select>	
	 -->
</mapper>


























