<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.bit.groupware.persistent.mapper.authority.PostMapper">
	<insert id="insertPost" parameterType="PostVO" statementType="PREPARED">
		<selectKey keyProperty="boardNo" resultType="int" statementType="STATEMENT" order="BEFORE">
			select post_no_seq.nextval from dual		
		</selectKey>
		<selectKey keyProperty="tmpNo" statementType="STATEMENT" order="BEFORE">
			select template_seq.nextval from dual
		</selectKey>
		insert into template(tmp_no,tmp_name,category_no,tmp_content,tmp_summary,tmp_using)
		values(#{tmpNo},#{tmpName},#{templateCategory.categoryNo},#{tmpContent},#{tmpSummary},#{tmpUsing});
	</insert>
</mapper>


























