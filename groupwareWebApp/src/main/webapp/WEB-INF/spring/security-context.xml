<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
<!-- 
	<security:http auto-config="true" use-expressions="true">	
		<security:intercept-url pattern="/passwordEncode.do" access="permitAll()" />
		<security:intercept-url pattern="/home.jsp" access="permitAll()" />					
		<security:intercept-url pattern="/admin/**"	access="hasRole('ROLE_ADMIN')" />
		<security:intercept-url pattern="/user/**"	access="authenticated()" />
		<security:form-login default-target-url="/home.jsp"/>
		<security:logout />		
	</security:http>

	<bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />


	DB에서 사용자 정보 관리
	<security:authentication-manager erase-credentials="true">
		<security:authentication-provider user-service-ref="userDetailsService">
			<security:password-encoder ref="passwordEncoder" />
		</security:authentication-provider>
	</security:authentication-manager>

 -->
 <!-- <security:http  use-expressions="true">		
		<security:custom-filter ref="filterSecurityInterceptor" before="FILTER_SECURITY_INTERCEPTOR"/>
		로그인폼 커스터마이징
		<security:form-login login-page="/login.do"
							 username-parameter="id" 
							 login-processing-url="/j_spring_security_check"
			                 password-parameter="pwd" default-target-url="/main.do" 
			                 authentication-failure-url="/login.do?fail=true"/>		
		로그아웃 커스터마이징
		<security:logout invalidate-session="true" logout-success-url="/main.do"/>
		<security:csrf disabled="true"/>		
	</security:http> -->
</beans>

































